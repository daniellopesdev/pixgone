[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Python build complete'"]

[start]
cmd = "uvicorn server_ormbg_only:app --host 0.0.0.0 --port $PORT" 